# Project Notes

## Completed Cleanup

- Renamed `INDEX.html` to `index.html`.
- Consolidated active CSS into `assets/css/app.css`.
- Moved the icon into `assets/icons/`.
- Moved JavaScript into `assets/js/`.
- Moved runtime config into `config/site-config.json`.
- Moved old and experimental files into `archive/`.
- Folded the config loader into `assets/js/app.js`.

## Possible Next Step

- Add a sound or notification when a timer session ends.
